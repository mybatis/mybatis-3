<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.apache.ibatis.submitted.add_localCacheDetails.UserDao">
    <select id="findAll" resultType="org.apache.ibatis.submitted.add_localCacheDetails.User">
      select * from user
    </select>

    <select id="findAll2" resultType="org.apache.ibatis.submitted.add_localCacheDetails.User"
          localCacheScope="STATEMENT">
      select * from user
    </select>

    <select id="findAll3" resultType="org.apache.ibatis.submitted.add_localCacheDetails.User"
          localCacheScope="NOUSE">
      select * from user
    </select>
</mapper>
