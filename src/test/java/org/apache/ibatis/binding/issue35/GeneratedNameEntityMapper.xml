<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.apache.ibatis.binding.issue35.GeneratedNameEntityMapper">

    <insert id="insert" parameterType="org.apache.ibatis.binding.issue35.NameEntity">

        insert into table1(id, name, age) values (#{id}, #{name}, #{age})

    </insert>

    <update id="update" parameterType="org.apache.ibatis.binding.issue35.NameEntity">

        update table1 set name = #{name}, age = #{age} where id = #{id}

    </update>

    <delete id="delete" parameterType="java.lang.Integer">

        delete from table1 where id = #{id}

    </delete>

    <select id="selectById" parameterType="java.lang.Integer" resultType="org.apache.ibatis.binding.issue35.NameEntity">

        select id, name, age from table1 where id = #{id}

    </select>

</mapper>